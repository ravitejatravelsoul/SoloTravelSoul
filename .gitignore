# ── Secrets (NEVER commit these) ─────────────────────────────────────
.env
.env.local
.env.production
.env.*.local
# Explicit sub-package paths (belt-and-suspenders — .env above covers these via glob)
apps/mobile/.env
apps/mobile/.env.local
google-services.json
GoogleService-Info.plist
google-play-key.json
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision

# ── Node ──────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Expo ──────────────────────────────────────────────────────────────
.expo/
dist/
web-build/
expo-env.d.ts

# ── EAS ───────────────────────────────────────────────────────────────
.eas/
eas-build-pre-install.sh

# ── Metro ─────────────────────────────────────────────────────────────
.metro-health-check*

# ── Build outputs ─────────────────────────────────────────────────────
*.orig.*
*.jsbundle

# ── OS files ──────────────────────────────────────────────────────────
.DS_Store
*.DS_Store
Thumbs.db
desktop.ini

# ── IDE ───────────────────────────────────────────────────────────────
.vscode/settings.json
.idea/
*.suo
*.user

# ── Xcode (old iOS project — keep for reference but ignore build artifacts) ──
build/
DerivedData/
*.xcuserstate
*.xcbkptlist
xcuserdata/

# ── Turborepo ─────────────────────────────────────────────────────────
.turbo/

# ── TypeScript ────────────────────────────────────────────────────────
*.tsbuildinfo
